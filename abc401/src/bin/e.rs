#[allow(unused_imports)]
use proconio::{fastout, input, marker::{Bytes, Chars, Usize1}};

#[allow(non_snake_case)]
#[fastout]
fn main() {
    input! {
        n: usize,
    }

    
    
    println!("{}", an);
}
